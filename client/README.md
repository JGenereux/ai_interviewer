How to build:
  1: cd ./backend 
  2: npm install && npm run dev
  3: cd ./client
  4: npm install && npm run dev

The backend is used to generate a unique key for a conversation. 
In ./backend create a .env with OPENAI_API_KEY='' variable using your openai api key.